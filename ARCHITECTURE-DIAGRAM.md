# 🏗️ Architecture & Deployment Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USERS                                   │
│                    (Worldwide Access)                           │
└────────────────────┬──────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
    ┌────────────┐          ┌────────────┐
    │  VERCEL    │          │  BROWSER   │
    │ Frontend   │  HTTPS   │   Cache    │
    │ (React)    │◄────────►│            │
    └────────────┘          └────────────┘
        │
        │ API Calls
        │ JSON
        │
        ▼
    ┌──────────────────────┐
    │   RENDER Platform    │
    │                      │
    │  ┌──────────────┐    │
    │  │   Django     │    │
    │  │   REST API   │    │
    │  │   (Python)   │    │
    │  └──────┬───────┘    │
    │         │            │
    │         ▼            │
    │  ┌──────────────┐    │
    │  │ PostgreSQL   │    │
    │  │  Database    │    │
    │  │              │    │
    │  │ • Users      │    │
    │  │ • Products   │    │
    │  │ • Orders     │    │
    │  │ • Wishlists  │    │
    │  └──────────────┘    │
    │                      │
    │  ┌──────────────┐    │
    │  │ Node.js      │    │
    │  │ Payments     │    │
    │  │ (Optional)   │    │
    │  └──────────────┘    │
    └──────────────────────┘
```

---

## Deployment Flow

```
Local Machine (Your Computer)
         ▼
    ┌─────────┐
    │  Git    │────► Push to GitHub
    │ Commit  │
    └─────────┘
         │
         ▼
  ┌──────────────┐
  │   GitHub     │
  │ Repository   │
  │  (giftology) │
  └──┬────────┬──┘
     │        │
     │        └────────┐
     │                 │
  [PUSH TRIGGER]    [PUSH TRIGGER]
     │                 │
     ▼                 ▼
  ┌─────────┐       ┌──────────┐
  │ RENDER  │       │ VERCEL   │
  │ (Backend)       │(Frontend)│
  │         │       │          │
  │ Build:  │       │ Build:   │
  │ Python  │       │ React    │
  │ Django  │       │          │
  │         │       │ Deploy:  │
  │ Test:   │       │ CDN Push │
  │ Migrate │       │          │
  │         │       │ Live At: │
  │ Deploy: │       │ vercel   │
  │ Gunicorn│       │ .app     │
  │         │       │          │
  │ Live At:│       └──────────┘
  │ onrender│
  │ .com    │
  └─────────┘
```

---

## Data Flow During User Journey

```
USER VISIT: https://giftology.vercel.app

┌─────────────┐
│  Browser    │
│  Loads HTML │
│   CSS, JS   │  ←── Vercel CDN (Fast!)
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│  React App Initializes  │
│  • AuthContext loaded   │
│  • CartContext loaded   │
│  • Check localStorage   │
└──────┬──────────────────┘
       │
       ├─► User Not Logged In
       │   ├─► Browse Products
       │   │   └─► API Call: /api/products/ ──┐
       │   │                                   │
       │   └─► Click Register                  │
       │       └─► API Call: /api/register/    │
       │                                       │
       └─► User Logged In ◄────────────────────┤
           ├─► API Call: /api/user/profile/   │
           │   └─► Auto-fill Checkout Form    │
           │                                   │
           ├─► View Wishlist                  │
           │   └─► API Call: /api/wishlist/   │
           │                                   │
           ├─► View Orders                    │
           │   └─► API Call: /api/orders/     │
           │                                   │
           ├─► Add to Cart                    │
           │   └─► localStorage (browser)     │
           │                                   │
           ├─► Checkout                       │
           │   └─► API Call: /api/orders/create/ ◄─┐
           │       ├─► Validate Form          │     │
           │       ├─► Create Order Object    │     │
           │       ├─► Save to Database       │     │
           │       └─► Return Success         │     │
           │                                   │     │
           └─► Order Confirmation          ◄──────┘
               └─► View in Orders Page
                   └─► API Call: /api/orders/


                    ↓↓↓ ALL API CALLS GO TO ↓↓↓
                    
┌─────────────────────────────────────┐
│  Render Backend                     │
│  https://giftology-backend.onrender │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Django REST Framework      │   │
│  │  giftology_backend/         │   │
│  │  ├── store/                 │   │
│  │  │   ├── views.py (API)     │   │
│  │  │   ├── models.py          │   │
│  │  │   ├── serializers.py     │   │
│  │  │   └── urls.py            │   │
│  │  └── settings.py            │   │
│  └──────┬──────────────────────┘   │
│         │                           │
│         ▼                           │
│  ┌─────────────────────────────┐   │
│  │  PostgreSQL Database        │   │
│  │  (on Render)                │   │
│  │                             │   │
│  │  Tables:                    │   │
│  │  • auth_user                │   │
│  │  • store_product            │   │
│  │  • store_order              │   │
│  │  • store_orderitem          │   │
│  │  • store_wishlistitem       │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## Authentication Flow

```
┌──────────────────┐
│  User Registers  │
└────────┬─────────┘
         │
         ▼
    ┌─────────────────────────────┐
    │ POST /api/register/         │
    │ {email, password, name}     │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Backend Creates User         │
    │ • Hash password             │
    │ • Save to Database          │
    │ • Return success/error      │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Frontend Shows Confirmation │
    │ • Save to localStorage      │
    │ • Update AuthContext        │
    │ • Redirect to Login         │
    └────────┬────────────────────┘
             │
             ▼
┌──────────────────┐
│  User Logs In    │
└────────┬─────────┘
         │
         ▼
    ┌─────────────────────────────┐
    │ POST /api/login/            │
    │ {email, password}           │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Backend Authenticates       │
    │ • Verify password hash      │
    │ • Return user info + token  │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Frontend Stores User        │
    │ • Save to localStorage      │
    │ • Update AuthContext        │
    │ • Update UI (show account)  │
    │ • Fetch user profile data   │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Now User Can:               │
    │ ✓ Add to cart               │
    │ ✓ View wishlist             │
    │ ✓ Checkout with auto-fill   │
    │ ✓ View orders               │
    │ ✓ Manage account            │
    └─────────────────────────────┘
```

---

## Checkout Flow (Pre-filled for Logged-in Users)

```
User Clicks "Checkout"
         │
         ▼
┌──────────────────────────────────┐
│  Checkout Component Mounts        │
│  useEffect(() => {               │
│    if (user && user.email)       │
│      fetchUserProfile()          │
│  })                              │
└────────┬─────────────────────────┘
         │
         ▼
    ┌─────────────────────────────┐
    │ GET /api/user/profile/      │
    │ ?email=user@email.com       │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Backend Queries Database     │
    │ • Find user by email        │
    │ • Get latest order          │
    │ • Return profile data       │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Frontend Auto-fills Form     │
    │ • email                     │
    │ • firstName                 │
    │ • lastName                  │
    │ • address                   │
    │ • city                      │
    │ • phone                     │
    │                             │
    │ (From previous orders OR    │
    │  from user profile)         │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ User Reviews Form            │
    │ • Can edit if needed        │
    │ • Sees auto-filled data     │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ User Clicks "Place Order"    │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ POST /api/orders/create/    │
    │ {                           │
    │  email,                     │
    │  first_name,                │
    │  last_name,                 │
    │  address,                   │
    │  city,                      │
    │  phone,                     │
    │  coupon_code,               │
    │  total_price,               │
    │  items: [{...}, {...}]      │
    │ }                           │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Backend Creates Order        │
    │ • Validate all fields       │
    │ • Create Order record       │
    │ • Create OrderItems         │
    │ • Return order_id           │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Frontend Shows Success       │
    │ • 🎉 Order Confirmation     │
    │ • Clear cart                │
    │ • Redirect to home          │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ User Can View Order          │
    │ • Go to "Orders" page       │
    │ • GET /api/orders/          │
    │ • See all past orders       │
    └─────────────────────────────┘
```

---

## Database Schema

```
┌─────────────────────────────────────────────┐
│         PostgreSQL Database                 │
│        (on Render: giftology-db)            │
└─────────────────────────────────────────────┘

┌──────────────────┐      ┌─────────────────┐
│  auth_user       │      │  store_product  │
├──────────────────┤      ├─────────────────┤
│ id (PK)          │      │ id (PK)         │
│ username         │      │ name            │
│ email            │      │ description     │
│ password_hash    │      │ price           │
│ first_name       │      │ image (URL)     │
│ last_name        │      │ stock           │
│ date_joined      │      │ created_at      │
│ is_active        │      └─────────────────┘
└────┬─────────────┘            ▲
     │                          │
     │ (1 to Many)              │ (Foreign Key)
     │                          │
     ▼                          │
┌──────────────────────┐  ┌─────────────────┐
│   store_order        │  │ store_orderitem │
├──────────────────────┤  ├─────────────────┤
│ id (PK)              │  │ id (PK)         │
│ user_id (FK) ─────┐  │  │ order_id (FK)───┐
│ total_price        │  │  │ product_id (FK)─┼──┐
│ address            │  │  │ quantity        │  │
│ city               │  │  └─────────────────┘  │
│ phone              │  │                       │
│ created_at         │  └─ (1 to Many) ────────┘
└──────────────────────┘

     ▲
     │ (1 to Many)
     │
┌────┴───────────────┐
│ store_wishlistitem │
├────────────────────┤
│ id (PK)            │
│ user_id (FK) ──┐   │
│ product_id (FK)├──►(Referenced Above)
│ added_at       │   │
└────────────────┘   │
                     └─ (Foreign Key to Product)
```

---

## Development vs Production

```
LOCAL DEVELOPMENT
─────────────────
┌─────────────────────┐
│  localhost:3000     │
│  (React Dev Server) │
└─────────────────────┘
         │
         ├─► Auto-refresh on code changes
         ├─► Hot Module Replacement (HMR)
         ├─► Full error messages
         ├─► No minification
         │
         ▼
    .env.local
    REACT_APP_API_URL=http://127.0.0.1:8000/api/
    
         │
         ▼
┌─────────────────────┐
│  localhost:8000     │
│  (Django Dev Server)│
│  python manage.py   │
│  runserver          │
└─────────────────────┘
         │
         ├─► SQLite Database
         ├─► DEBUG=True
         ├─► Detailed error pages
         ├─► Live code reloading
         │
         ▼
    CORS_ALLOW_ALL_ORIGINS = True
    (for local testing)


PRODUCTION DEPLOYMENT
──────────────────────
┌──────────────────────┐
│  giftology.vercel.app│
│  (Vercel CDN)        │
│  Optimized Build     │
└──────────────────────┘
         │
         ├─► Minified JavaScript
         ├─► Optimized CSS
         ├─► Tree-shaken imports
         ├─► Gzip compression
         │
         ▼
    .env.production
    REACT_APP_API_URL=https://giftology-backend.onrender.com/api/
    
         │
         ▼
┌──────────────────────────────┐
│ giftology-backend.onrender   │
│ (Gunicorn WSGI Server)       │
│ cd backend-django &&         │
│ gunicorn giftology_backend   │
│ .wsgi:application            │
└──────────────────────────────┘
         │
         ├─► PostgreSQL Database (managed by Render)
         ├─► DEBUG=False
         ├─► WhiteNoise static files
         ├─► Restricted CORS origins
         │
         ▼
    ALLOWED_HOSTS = [
        'giftology-backend.onrender.com'
    ]
    
    CORS_ALLOWED_ORIGINS = [
        'https://giftology.vercel.app'
    ]
```

---

## File Structure After Deployment Setup

```
giftology/
│
├── 📄 DEPLOYMENT-README.md (YOU ARE HERE)
├── 📄 DEPLOYMENT-GUIDE.md (Detailed)
├── 📄 STEP-BY-STEP-DEPLOYMENT.md (Visual)
├── 📄 DEPLOYMENT-QUICK-GUIDE.md (Quick Ref)
├── 📄 DEPLOYMENT-COMMANDS.md (Copy-Paste)
├── 📄 render.yaml (Render config)
├── 📄 README.md (Project info)
│
├── 📁 backend-django/
│   ├── 📄 requirements.txt ✅ UPDATED
│   ├── 📄 manage.py
│   ├── 📁 giftology_backend/
│   │   ├── 📄 settings.py ✅ UPDATED
│   │   ├── 📄 urls.py
│   │   ├── 📄 asgi.py
│   │   └── 📄 wsgi.py
│   ├── 📁 store/
│   │   ├── 📄 models.py
│   │   ├── 📄 views.py
│   │   ├── 📄 urls.py
│   │   └── ... other files
│   └── 📄 db.sqlite3
│
├── 📁 frontend/
│   ├── 📄 package.json
│   ├── 📄 .env.production ✅ CREATED
│   ├── 📄 .env.local ✅ CREATED
│   ├── 📄 vercel.json ✅ UPDATED
│   ├── 📁 src/
│   │   ├── 📄 api.ts ✅ UPDATED
│   │   ├── 📄 App.tsx
│   │   ├── 📁 pages/
│   │   ├── 📁 components/
│   │   ├── 📁 context/
│   │   └── ... other files
│   └── 📁 public/
│
└── 📁 backend-node/
    ├── 📄 package.json
    ├── 📄 server.js
    └── ... other files
```

---

## Timeline: How Long Will It Take?

```
TOTAL TIME: ~30 minutes (first time)

├─ 5 mins: Push to GitHub
├─ 10 mins: Deploy Django backend (Render)
│          ├─ Create database
│          ├─ Create web service
│          └─ Wait for build
├─ 2 mins: Update frontend URL
├─ 5 mins: Deploy React frontend (Vercel)
├─ 5 mins: Test everything
└─ 3 mins: Celebrate! 🎉

AFTER DEPLOYMENT:
├─ Create admin user (~2 mins)
├─ Add products (~5-10 mins)
└─ Test full workflow (~5 mins)

TOTAL WITH SETUP: ~1 hour
```

---

## Summary

```
YOUR GIFTOLOGY APP

┌─────────────────────────────────────────┐
│         FULLY DEPLOYED & LIVE            │
├─────────────────────────────────────────┤
│                                         │
│  Frontend (React)  ← Vercel             │
│  Backend (Django)  ← Render             │
│  Database (PostgreSQL) ← Render         │
│                                         │
│  URL: https://giftology.vercel.app     │
│                                         │
│  ✓ Always accessible worldwide         │
│  ✓ Real database (not SQLite)          │
│  ✓ Professional setup                   │
│  ✓ Ready for real users                │
│  ✓ Scalable architecture               │
│                                         │
└─────────────────────────────────────────┘
```

---

**Ready to deploy? Start with STEP-BY-STEP-DEPLOYMENT.md!** 🚀
